#include "models/bpp_models/bpp_instance.hpp"

#include <vector>

using namespace models::bpp_models;
using namespace std;

BPP_instance::BPP_instance(vector<double> items_, double capacity_) : items(items_), capacity(capacity_) {
}

